---
name: Hooks
route: /hooks
---

import { useState } from "react";
import { Playground } from "docz";

import ReactGlobe from "./ReactGlobe";
import markers from "./markers";

# Hooks

`ReactGlobe` provides the `useGlobe` hook that exposes the `Globe` instance. This is useful if you

<Playground>
  {() => {
    const [globeInstance, setGlobeInstance] = useState();
    const [focus, setFocus] = useState();
    const [lightOptions, setLightOptions] = useState({});
    return (
      <div>
        <p>
          <b>Edit Globe Instance</b>
          <br />
          <button onClick={() => globeInstance.freeze()}>Freeze</button>
          <button onClick={() => globeInstance.unfreeze()}>Unfreeze</button>
          <br />
          <button onClick={() => globeInstance.updateFocus([1.3521, 103.8198])}>
            Change Focus
          </button>
          <button onClick={() => globeInstance.updateFocus(undefined)}>
            Unfocus
          </button>
          <button
            onClick={() =>
              globeInstance.updateLights({ ambientLightColor: "red" })
            }
          >
            Change lights
          </button>
          <button
            onClick={() =>
              globeInstance.applyAnimations([
                {
                  animationDuration: 5000,
                  coordinates: [1.3521, 103.8198],
                  distanceRadiusScale: 2,
                  easingFunction: ["Linear", "None"]
                },
                {
                  animationDuration: 3000,
                  coordinates: [39.9042, 116.4074],
                  distanceRadiusScale: 2,
                  easingFunction: ["Cubic", "InOut"]
                },
                {
                  animationDuration: 3000,
                  coordinates: [37.773972, -122.431297],
                  distanceRadiusScale: 2,
                  easingFunction: ["Back", "InOut"]
                }
              ])
            }
          >
            Apply animations
          </button>
          <br />
        </p>
        <p>
          <b>Edit Globe Props</b>
          <br />
          <button onClick={() => setFocus([37.773972, -122.431297])}>
            Change Focus
          </button>
          <button onClick={() => setFocus(undefined)}>Defocus</button>
          <button
            onClick={() => setLightOptions({ ambientLightColor: "blue" })}
          >
            Change lights
          </button>
        </p>
        <ReactGlobe
          focus={focus}
          lightOptions={lightOptions}
          markers={markers}
          onGetGlobeInstance={setGlobeInstance}
        />
      </div>
    );
  }}
</Playground>
