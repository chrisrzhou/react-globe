import {
  CameraOptionsProp as Props,
  defaultCameraOptions,
} from '../usage/react-globe';

export default function CameraOptionsProp(_props: Props): JSX.Element {
  return null;
}

CameraOptionsProp.defaultProps = defaultCameraOptions;
