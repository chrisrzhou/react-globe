import { CameraOptions as Props, defaultCameraOptions } from '../../../src';

export default function CameraOptions(_props: Props) {
  return null;
}

CameraOptions.defaultProps = defaultCameraOptions;
