import {
  LightOptionsProp as Props,
  defaultLightOptions,
} from '../usage/react-globe';

export default function LightOptionsProp(_props: Props): JSX.Element {
  return null;
}

LightOptionsProp.defaultProps = defaultLightOptions;
